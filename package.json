{
  "name": "ai-careeros",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"cd backend && npm run dev\" \"cd landing-page && npm run dev\"",
    "build": "cd landing-page && npm run build && cd ../backend && npm run build",
    "start": "cd backend && npm run start",
    "seed": "cd backend && npm run seed"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.14.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "concurrently": "^9.1.2"
  }
}
